{
  "name": "balkantransport",
  "version": "1.0.0",
  "scripts": {
    "dev": "hugo server -s site --disableFastRender",
    "build": "hugo -s site --gc --minify"
  },
  "devDependencies": {
    "hugo-extended": "0.121.0"
  }
}
